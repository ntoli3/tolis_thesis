function [enriched_nodes] = find_enriched_nodes(element_nodes, intersected_elements)
%
% enriched_nodes = (num_enchriched_nodes x 1) 

num_elements = size(element_nodes, 1);

enriched_nodes = [];

for e = 1:num_elements
    is_intersected = (intersected_elements(e) == 0);
    
    if is_intersected
        for n = 1:4
            enriched_nodes(end+1, 1) =  
        end
    end
end

end