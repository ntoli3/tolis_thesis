- Fix the ramp enrichment. Right now, both regions od an intersected element behave the same (blending and std elements are ok).
- Check that FEM and XFEM (with same material for both regions) give the same results
- Cohesive interface. Different than sign enrichment. Needs integration over the intersection segments
- Level set for multiple inclusions
- Delete old files